# Handoff — Sessão 25/05/2026

**Commit principal:** `dd9f7f8`
**Deploy:** https://terra-gestao-financeira.vercel.app (verificado hash a hash pós-push)
**Documento de origem:** `Correções Sistema_Terra.docx` (Juliana, 25/05) — tópicos 1–3.

---

## Resumo do pacote entregue

Pacote único cobrindo correções de UX e estrutura em **Cargos**, **Organograma** e **Funcionários**, mais aba nova de configuração para alerta automático de ASO. Migrations M34 e M35 aplicadas ao Supabase em produção.

### Banco (M34 + M35)

**M34 — Cargos + Organograma + Config**
- `cargos`: novas colunas `status_publicacao` (rascunho/publicado), `eh_gestao` (boolean), `comp_tec_conhecimentos`, `comp_tec_habilidades`, `comp_comp_competencias`, `comp_comp_atitudes`. Cargos antigos com dados foram migrados pra `publicado`; o texto antigo de `competencias` migrou pra `comp_tec_conhecimentos`.
- `organograma`: novas colunas `funcionario_id` (FK), `status_oficializacao` (pendente/oficializado), `oficializado_em`, `oficializado_por`. Posições que já tinham profissional foram marcadas como `oficializado` automaticamente.
- Nova tabela `config_sistema` (key/value, RLS write restrita a perfil master).

**M35 — Alerta ASO + pg_cron**
- Tabela `alertas_aso`.
- Função `gerar_alertas_aso_diario()` (SECURITY DEFINER): varre funcionários ativos com `vencimento_aso` entre hoje e +30 dias, faz upsert em `alertas_aso`.
- Job `cron.schedule('alerta_aso_diario', '0 11 * * *', ...)` rodando diariamente às **11h UTC (8h de Brasília)**.

### Cargos

- Modal redesenhado com 4 seções de competências separadas: **Técnicas** (Conhecimentos + Habilidades) e **Comportamentais** (Competências + Atitudes), conforme imagens do doc.
- Multi-select de subordinados (atualiza reversamente o `superior_id` dos cargos selecionados — mantém modelo 1:N original).
- Flag "É cargo de gestão" — quando ligada, exige ao menos 1 subordinado para publicar.
- Botões **"Salvar como rascunho"** (default, permite parcial) e **"Publicar"** (exige nome, depto, superior, missão, responsabilidades, as 4 colunas de competências e, se gestão, ≥1 subordinado).
- Lista de cargos com badges visuais `PUBLICADO` / `RASCUNHO` / `GESTÃO`, filtro por status, linha clicável.
- **Botão "Ficha PDF"** por cargo: gera PDF de ciência com logo Terra, quadros 2 colunas para cada categoria de competência, missão, responsabilidades, considerações e bloco de assinatura (Profissional / Empresa, com campos de data).
- Cargo em rascunho **não aparece** nos dropdowns de Funcionários nem em outros lugares.

### Organograma

- Campo "Profissional" agora é **dropdown de funcionários ativos** (era texto livre).
- Workflow: ao vincular, posição entra como `PENDENTE` (badge amarelo, borda tracejada). Botão **"Oficializar"** aparece somente pra quem ocupa o cargo **"Analista de RH Generalista"** (id=2) ou perfil master.
- Trocar funcionário de uma posição oficializada volta pra `PENDENTE` automaticamente.
- **Bug "Baixar PDF" corrigido**: agora valida que o organograma carregou antes de gerar; esconde `org-status`, botões "Oficializar" e toggles durante captura `html2canvas`; restaura UI ao final.

### Funcionários

- **Bug filtro estourando** resolvido: `autoEnhanceLargeSelects` agora ignora selects dentro de `.toolbar` (causava wrapper column inflando o card).
- Modal abre clicando na linha inteira (botão "Editar" mantido como atalho rápido).
- **Pop-up "Suas informações não estão salvas"** ao fechar modal com alterações pendentes — opções Sair / Salvar / Cancelar. Se "Salvar" tem erro de validação, pop-up fecha e modal volta destacando o erro com dados preservados.
- Campo "Função / Cargo (texto livre — legado)" **removido**. Dropdown "Cargo" agora lista somente publicados. A coluna texto `cargo` (mantida no DB) passa a ser derivada do `cargo_id` no save.
- Lista: colunas **Centro de Custo** e **Posição (organograma)** removidas (continuam editáveis no modal).
- **E-Social** vira subtítulo do nome na lista + entra na busca textual.
- Cards "Total" e "Folha Mensal Base" removidos (mantidos hidden pra JS não quebrar); "Ativos" compactado.
- Cabeçalho da tabela **fixo** via `.thead-sticky`.

### UX geral

- Campos em foco ganham **fundo branco + borda dourada** (sinaliza edição).
- **Quattrocento** aplicada universalmente via CSS forte (cobre componentes que escapavam).
- Ficha cadastral em modais: **grid 2 colunas** dentro de cada fieldset, labels mais legíveis, espaçamento revisado — sem encavalar linhas.
- Framework de modal estendido: tipos `multiselect` e `checkbox`, suporte a `extraButtons` (botões adicionais que disparam `onSubmit` com `action` própria), tracking de `dirty` pro pop-up de saída.

### Configurações → 🩺 Alerta ASO (novo)

- Novo cartão no menu Configuração.
- Campo de email destinatário (persiste em `config_sistema`).
- Botão "Gerar alertas agora" (chama RPC `gerar_alertas_aso_diario()` na hora — útil pra testar).
- Lista de alertas pendentes com dias restantes coloridos: ≤7 vermelho, ≤15 laranja, ≤30 marrom.
- Status do email aparece como **"Pendente envio"** porque ainda **não há provedor SMTP configurado** — ver pendência abaixo.

---

## Pendências para a próxima sessão

1. **Envio real do email do alerta ASO**: o cron está gerando os alertas todos os dias, mas o envio em si depende de provedor (Resend, SendGrid ou SMTP customizado). Quando a Juliana definir o provedor, criar uma Edge Function `alerta-aso-enviar` que lê `alertas_aso WHERE email_enviado=false`, dispara o email pro destinatário em `config_sistema.alerta_aso_destinatario`, e marca `email_enviado=true`. Esta edge function pode ser chamada por outro pg_cron (mesma janela 8h) ou rodar em paralelo.

2. **Vincular alguém ao cargo "Analista de RH Generalista"** (id=2). Hoje o cargo existe mas nenhum funcionário está vinculado. Sem isso, o botão "Oficializar" no organograma só aparece pra perfil master.

3. **Tópicos 4+ do documento de correções** (a Juliana mandou só os 3 primeiros — Organograma, Cargos, Funcionários). Quando vier o restante, continuar daqui.

4. **Validação visual da Quattrocento**: aplicada via `!important` universal, mas pode ter algum componente bem específico (timeline, gráficos Chart.js) que ainda use Arial/sans-serif por estilo inline. Reportar se aparecer.

---

## Como retomar

1. `git clone https://github.com/Natalia-Terra/terra-gestao-financeira` no sandbox novo.
2. Para deploy: usar PAT da Juliana (não persiste entre sandboxes — solicitar ao retomar).
3. Banco Supabase: project_id `zvvdpdldjmzuzieinxwa` (Terra-Gestão-Financeira).
4. Vercel: o deploy é automático por push na branch `main` (julianapolimatas/terra-gestao-financeira não está em conta acessível via MCP — verificação de deploy é feita por hash do conteúdo servido).
5. Checklist completo de teste deste pacote: `docs/CHECKLIST_TESTE_25-05.md`.
