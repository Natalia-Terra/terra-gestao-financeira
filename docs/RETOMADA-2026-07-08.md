# Retomada — 08/07/2026

Registro da sessão de retomada do trabalho de migração/gestão financeira da Terra Conttemporânea.

## Estado das conexões
- **Supabase → org Terra Conttemporanea** (`pnnrurshttjawauamgkv`) reautorizado; a org Polímata deixou de aparecer, como esperado.
- **Vercel:** conta pessoal julianapolimata só enxerga projetos Polímata; o deploy da Terra vive sob outra conta/time.
- **GitHub:** conta da Terra é **Natalia-Terra** (login separado da Polímata — não misturar sessões).

## App (front)
- **Produção:** https://terra-gestao-financeira.vercel.app (confirmado no ar)
- **Repositório (fonte de verdade):** Natalia-Terra/terra-gestao-financeira
- Stack: Vanilla JS + Supabase JS SDK v2 (CDN) + SheetJS; deploy Vercel (Hobby).

## Dados no Supabase (projeto Terra-Gestão-Financeira `zvvdpdldjmzuzieinxwa`)
Contagens conferidas nesta sessão, batendo com a carga original:

| Tabela | Registros |
|---|---|
| movimentos | 1.436 |
| orcamentos | 282 |
| saldo_reconhecer | 194 |
| estoque_detalhes | 13.149 |
| estoque_resumo | 773 |

Carga versionada: `imports_historico` id=1, `importacoes` id=2.

## Pendências
1. **Excluir Edge Function `bulk-load`** (temporária, já desativada/410) pelo painel do Supabase. Manter a `gerenciar-usuarios`.
2. **Conferir telas:** logar no app e validar que os números carregados aparecem.
3. **Estoque sem OS:** saídas sem "Código OS" → `codigo_os = 'SEM_OS'`; movimentos sem orçamento (11) → `orcamento = 'SEM_ORC'`.
